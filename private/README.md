# private

Use this directory for private parent-repo files such as:

- integration notes
- local config templates
- scripts that orchestrate the two submodules
- private application code that depends on the submodules

Do not put these files inside the child submodules unless the change is meant to live in those repos themselves.
